#signup
	= simple_form_for(resource, :as => resource_name, :url => registration_path(resource_name)) do |f|
		= #f.error_notification

		.row
			.form-inputs
				.small-6.small-centered.columns
					= f.input :email, :label => 'Email', :required => true, :autofocus => true
		.row
			.small-3.small-offset-3.columns
				= f.input :password, :label => 'Passwort', :required => true
			.small-3.columns.end
				= f.input :password_confirmation, :label => 'Passwort Bestätigung', :required => true

		.row
			.form-actions
				.small-6.small-centered.columns
					= f.button :submit, "Registrieren", :class => :expand
